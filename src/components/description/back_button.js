import React from 'react';

const BackButton = () => {
  return(
    <div className="back_button">
      <a href="/" className="back-to-home"><i className="glyphicon glyphicon-chevron-left"></i>back to home</a>
    </div>
  )
}

export default BackButton;
